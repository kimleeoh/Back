const rewardOtherCheck = async (type, data, temporarySessionData) => {
  try {
  } catch (e) {
    console.error(e);
    res.status(500).send("Internal Server Error");
  }
};
export { rewardOtherCheck };
